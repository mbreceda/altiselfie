import { ComponentMultiStyleConfig } from "@chakra-ui/react";

const MainContainer: ComponentMultiStyleConfig = {
  parts: ["main_container"],
  baseStyle: {
    main_container: {
      height: "calc(100vh)",
    },
  },
};

export default MainContainer;
